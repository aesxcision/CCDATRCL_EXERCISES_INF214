import java.util.LinkedList;
import java.util.Queue;

public class App {

    public static void main(String[] args) {
        Queue<String> SchoolID = new LinkedList<>();

        // Enqueue 5 of your classmates in this section
        SchoolID.add("Jeremiah");
        SchoolID.add("Jenina");
        SchoolID.add("Mariel");
        SchoolID.add("Jonnah");
        SchoolID.add("Sheandey");
       
       // Displays all the elements of the queue
        System.out.println("\nThese are all the elements of the queue: ");
        System.out.println(SchoolID);

        //Displays the size of the queue
        System.out.println("\nThe size of the queue is: ");
        System.out.println(SchoolID.size());

        //Shows the front of the queue
        System.out.println("\nThe front of the queue is: ");
        System.out.println(SchoolID.peek());

        //Dequeues 5 elements on the queue
        SchoolID.remove();
        SchoolID.remove();
        SchoolID.remove();
        SchoolID.remove();
        SchoolID.remove();

         // Enqueue 5 of your classmates in this section
         SchoolID.add("Jap");
         SchoolID.add("Jean");
         SchoolID.add("John");
         SchoolID.add("Kean");
         SchoolID.add("Neal");
        
        // Displays all the elements of the queue
         System.out.println("\nThese are all the elements of the queue: ");
         System.out.println(SchoolID);
 
         //Displays the size of the queue
         System.out.println("\nThe size of the queue is: ");
         System.out.println(SchoolID.size());
 
         //Shows the front of the queue
         System.out.println("\nThe front of the queue is: ");
         System.out.println(SchoolID.peek());
 }
}
